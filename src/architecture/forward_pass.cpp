#include <iostream>
#include <vector>
#include <cmath>
#include "../math/matrix.hpp"

class ForwardPass
{
    private:
        int input_size;
        int output_size;
        int batch_size;

    public:
        ForwardPass(int input, int output, int batch)
        {
            Matrix y(batch, output);

            y = 
        }
};



